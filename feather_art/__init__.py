"""羽画：离线把位图描成纯 HTML + CSS 单文件插画。

出生起就定了规矩——成品只有 div 和内联样式，能看见的图形全来自
CSS 多边形和渐变；img、SVG、Canvas、JavaScript、base64、外链一概不放行。
打开它不需要联网，也不需要相信任何人。

另一个脾气是确定性：同样的输入、参数和依赖环境，产出逐字节一致；
不搞随机，不玩玄学。

算法思路对过 AvroraCL 的 image-to-css-art（MIT），重写实现、没抄代码；
来历和致谢见 README.md。

命名这本账：
- 描摹就叫 trace（羽画的「画」）；动画里跨帧串线的图层轨迹叫 track；
- 草稿、半成品叫 draft；打底的薄纱叫 veil；微弱的位移漂移叫 drift；
- 新代码选名字往这几样上靠，老名字是四方锁定的契约，谁也不许动。
"""

__version__ = "0.2.0"
